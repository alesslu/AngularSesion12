import { TransformdatePipe } from './transformdate.pipe';

describe('TransformdatePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformdatePipe();
    expect(pipe).toBeTruthy();
  });
});
